<h1>All Tweets</h1>
<ul>
  <% Tweet.all.each do |tweet| %>
  <li>
    <!-- <h2><a href="/tweets/<%= tweet.id %>"><%= tweet.title %></a></h2> -->
    <h2><%= link_to(tweet.title, tweet) %></h2>
    <p><%= tweet.body %></p>
    <p>At: <%= tweet.location %></p>
  </li>
  <% end %>
</ul>
